# 問題修正 (2026-03-10)

## 修正的問題

### 1. ✅ Google Maps 短連結無法解析

**問題：**
```
URL: https://maps.app.goo.gl/W1m1PgsPt6KbMffQ6
錯誤：無法解析 Google Maps 連結
```

**原因：**
短連結需要重定向才能取得完整 URL，原本的實作只做正則表達式匹配，沒有處理重定向。

**解決方案：**
添加 `parseGoogleMapsUrlWithRedirect()` 方法：
1. 先嘗試直接解析
2. 如果是短連結（`maps.app.goo.gl` 或 `goo.gl/maps`），使用 `fetch` 跟隨重定向
3. 解析重定向後的完整 URL

**測試結果：**
```bash
npx ts-node test-google-maps-url.ts
```

```
✅ Short link: https://maps.app.goo.gl/W1m1PgsPt6KbMffQ6
   → 25.0726474, 121.5815906
```

**修改檔案：**
- `supabase/functions/_shared/tdx-client.ts`
- `supabase/functions/telegram-webhook/index.ts`

---

### 2. ✅ 試用模式文字修改

**修改前：**
```
💡 試用模式：今日已使用 1/2 次
使用 /setup 設定你的 API Key 以解除限制
```

**修改後：**
```
💡 今日已試用 1/2 次
請申請 TDX API Key 並使用 /setup 設定
```

**修改檔案：**
- `supabase/functions/telegram-webhook/index.ts`

---

### 3. ✅ Telegram Bot Commands 列表

**問題：**
在 Telegram 輸入 `/` 沒有看到指令列表

**原因：**
這是 Telegram 的快取機制，不是程式問題。Commands 已經成功設定，但 Telegram 需要重新載入才會顯示。

**驗證：**
```bash
npx ts-node scripts/verify-bot-commands.ts
```

**輸出：**
```
✅ Bot commands retrieved successfully!

📋 Current commands:
  /start - 開始使用
  /help - 查看說明
  /parking - 搜尋附近停車位
  /setup - 設定 TDX API Key
  /config - 查看當前配置
  /reset - 重置配置

✅ Total: 6 commands
```

**解決方案：**

#### 方法 1: 重新開啟對話（推薦）
1. 在 Telegram 切換到其他對話
2. 再切換回 Bot 對話
3. 輸入 `/` 應該就能看到指令列表

#### 方法 2: 刪除並重新開始對話
1. 刪除與 Bot 的對話
2. 重新搜尋並開啟 Bot
3. 發送 `/start`
4. 輸入 `/` 查看指令列表

#### 方法 3: 使用 BotFather（備用）
1. 開啟 [@BotFather](https://t.me/botfather)
2. 發送 `/setcommands`
3. 選擇你的 Bot
4. 輸入指令列表

**參考文件：**
- `BOT_COMMANDS_TROUBLESHOOTING.md` - 詳細疑難排解

**新增檔案：**
- `scripts/verify-bot-commands.ts` - 驗證腳本
- `BOT_COMMANDS_TROUBLESHOOTING.md` - 疑難排解文件

---

## 測試腳本

### 1. 測試 Google Maps URL 解析
```bash
npx ts-node test-google-maps-url.ts
```

測試以下格式：
- ✅ 短連結 (maps.app.goo.gl)
- ✅ 標準格式 (@lat,lon)
- ✅ Place 格式
- ✅ Query 參數

### 2. 驗證 Bot Commands
```bash
npx ts-node scripts/verify-bot-commands.ts
```

確認 6 個指令都已設定。

### 3. 設定 Bot Commands
```bash
npx ts-node scripts/setup-bot-commands.ts
```

如果需要重新設定指令。

---

## 修改的檔案

### 1. supabase/functions/_shared/tdx-client.ts
- ✅ 添加 `parseGoogleMapsUrlWithRedirect()` 方法
- ✅ 支援短連結重定向

### 2. supabase/functions/telegram-webhook/index.ts
- ✅ 更新 `handleMapsUrl()` 使用新方法
- ✅ 修改試用模式文字

### 3. scripts/setup-bot-commands.ts
- ✅ 修正 TypeScript 類型錯誤

---

## 新增的檔案

1. `test-google-maps-url.ts` - Google Maps URL 解析測試
2. `scripts/verify-bot-commands.ts` - Bot Commands 驗證腳本
3. `BOT_COMMANDS_TROUBLESHOOTING.md` - Bot Commands 疑難排解
4. `FIXES_2026-03-10.md` - 本文件

---

## 部署檢查清單

- [x] 修正 Google Maps 短連結解析
- [x] 修改試用模式文字
- [x] 設定 Bot Commands
- [x] 驗證 Bot Commands
- [x] 測試 Google Maps URL 解析
- [ ] 提交程式碼到 GitHub
- [ ] 等待 GitHub Action 部署
- [ ] 在 Telegram 測試

---

## 測試步驟

### 1. 測試 Google Maps 短連結
```
1. 在 Telegram 發送 /parking
2. 選擇距離
3. 貼上短連結: https://maps.app.goo.gl/W1m1PgsPt6KbMffQ6
4. 應該能正常解析並顯示停車場
```

### 2. 測試試用模式文字
```
1. 使用未設定 API Key 的帳號
2. 發送 /parking 並查詢
3. 結果應顯示: 💡 今日已試用 1/2 次
                請申請 TDX API Key 並使用 /setup 設定
```

### 3. 測試 Bot Commands
```
1. 在 Telegram 切換到其他對話
2. 切換回 Bot 對話
3. 輸入 /
4. 應該看到 6 個指令列表
```

---

## 已知限制

### Google Maps 短連結
- ✅ 支援 `maps.app.goo.gl` 短連結
- ✅ 支援 `goo.gl/maps` 短連結
- ⚠️ 需要網路連線才能跟隨重定向
- ⚠️ 如果 Google 改變重定向機制可能會失效

### Bot Commands
- ✅ Commands 已正確設定
- ⚠️ Telegram 有快取機制，需要重新開啟對話才會更新
- ⚠️ 在群組中的行為可能不同

---

## 參考資料

- [Telegram Bot API - setMyCommands](https://core.telegram.org/bots/api#setmycommands)
- [Telegram Bot API - getMyCommands](https://core.telegram.org/bots/api#getmycommands)
- [Fetch API - Redirect](https://developer.mozilla.org/en-US/docs/Web/API/Fetch_API/Using_Fetch#supplying_request_options)

---

**更新日期：** 2026-03-10  
**狀態：** ✅ 已修正並測試
